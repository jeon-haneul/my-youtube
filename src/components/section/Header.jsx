import React from "react";

const Header = () => {
    return (
        // role은 웹 애플리케이션의 접근성을 개선하기 위해 사용하는 속성이다.
        // banner는 로고, 제목 또는 헤더와 같이 웹 페이지의 핵심 콘텐츠를 나타낸다.
        <header id="header" role="banner">
            Header
        </header>
    );
};

export default Header;
